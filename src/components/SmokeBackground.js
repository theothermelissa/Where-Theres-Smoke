import React from "react";
import styled from "styled-components";
import { lightestgray, mediumgray, charcoal, mint, lightmint, coral, lightcoral, cream } from "./colors"

const SmokeBox = styled.div`
  position: fixed;
  margin: 0px;
  height: 80vh;
  width: 100%;
  overflow: hidden;
  background: ${mediumgray};
`;

